# Patterns

Reserved for business-neutral multi-component patterns.

Patterns may describe reusable layout recipes, but they must not depend on feature models from Auth, Me, Billing, Offers, Mall, Video, Weather, AI, or other modules. If a composition is only used by one feature, keep it in that feature module.
